# commented due not in use FOR NOW
name: Merge master to other branches
on:
  push:
    branches:
      - 'master'
jobs:
  sync-branch:
    runs-on: ubuntu-latest
    steps:
      - name: Skipping
        run: |
          echo "Skipping as we have no version branch to merge right now"
     
# UNCOMMENT AND CHANGE NEW BRANCH NAME WHEN NEEDED
#         runs-on: ubuntu-latest
#         steps:
#             - uses: actions/checkout@master

#             - name: Merge master -> version_4.2.1
#               uses: devmasx/merge-branch@v1.3.0
#               with:
#                   type: now
#                   head_to_merge: ${{ github.ref }}
#                   target_branch: version_4.2.1
#                   github_token: ${{ github.token }}
